cmake_minimum_required(VERSION 2.8)
project( compareHistograms )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( compareHistograms compareHistograms.cpp )
target_link_libraries( compareHistograms ${OpenCV_LIBS} )
