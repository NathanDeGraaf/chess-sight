cmake_minimum_required(VERSION 2.8)
project( compareHistogramsPieces )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( compareHistogramsPieces compareHistogramsPieces.cpp )
target_link_libraries( compareHistogramsPieces ${OpenCV_LIBS} )
