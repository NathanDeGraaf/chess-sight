cmake_minimum_required(VERSION 2.8)
project( filterPieces )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( filterPieces filterPieces.cpp )
target_link_libraries( filterPieces ${OpenCV_LIBS} )
